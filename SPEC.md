# MissionPlan 스펙

2인 스터디용 미션 트래커. 각자 일일 미션을 등록하고, 못 지킨 항목에 벌금을 누적한 뒤
7일 주기마다 정산한다. 각자 자기 PC에서 앱을 실행하고 DB만 공유한다.

---

## 1. 용어

| 용어 | 의미 |
|---|---|
| **Room** | 영구적인 스터디 공간. 초대코드가 여기 붙는다. 한 번 초대하면 계속 함께 쓴다 |
| **Member** | 방 참여자. 최대 2명 |
| **주기(Cycle)** | 7일 단위 정산 구간. **테이블 없이 날짜로 계산한다** |
| **Mission** | 특정 날짜에 속한 개인 할 일 |

### 주기 계산

주기는 저장하지 않는다. `Room.startDate`를 기준으로 나눗셈하면 나온다.

```
주기번호   = (미션날짜 − Room.startDate) / 7        // 0부터
주기시작일 = Room.startDate + 주기번호 × 7
주기종료일 = 주기시작일 + 6
```

덕분에 "이번 주기가 끝나면 뭘 해야 하나" 같은 처리가 없다. 다음 주 날짜로 미션을 등록하면
자동으로 다음 주기가 시작된다. 미션이 하나도 없는 주기는 벌금 0으로 자연히 처리된다.

---

## 2. 기본 규칙

- 미션은 **각자 자기 것만 등록하고 자기가 체크**한다. 공통 미션은 없다.
- 상대방의 미션과 진행 상황은 **열람만** 가능하다. 수정 불가.
- 미션은 특정 날짜에 귀속된다. 등록한 날짜가 곧 마감일이다.
- 완료 상태는 되돌릴 수 있다(체크 해제). 벌금은 항상 현재 상태로부터 다시 계산된다.
- 과거 기록은 삭제하지 않는다. 캘린더로 언제든 열람한다.

---

## 3. 벌금 계산

### 3.1 정의

매일 자정에 미완료 항목을 판정하고, 판정될 때마다 1,000원이 누적된다.

```
cutoff     = 완료했으면 (완료일 − 1일)
             미완료면   min(어제, 그 미션이 속한 주기의 종료일)

누적일수    = max(0, cutoff − 미션날짜 + 1)

벌금       = 누적일수 × 1,000원
```

### 3.2 성질

- **마감 당일은 유예된다.** 그날 자정을 넘겨야 첫 1,000원이 붙는다.
- **상한은 7,000원이다.** 주기 첫날 미션이 끝까지 미완료일 때가 최대(7일)이며,
  공식상 자연히 7을 넘지 않으므로 **별도 상한 처리 코드가 필요 없다.**
- **주기 종료 후로는 금액이 늘지 않는다.** cutoff를 주기 종료일로 clamp하기 때문.
  정산이 끝난 뒤에도 금액이 계속 불어나는 일이 없어야 한다.
- **뒤늦게 완료하면 그 시점에 카운트가 멈춘다.** 이미 쌓인 금액은 사라지지 않는다.

### 3.3 검산표 (주기가 월요일 시작인 경우)

| 미션 | 상황 | 누적일수 | 금액 |
|---|---|---|---|
| 월요일 미션 | 당일 완료 | 0 | 0원 |
| 월요일 미션 | 오늘이 월요일, 미완료 (유예) | 0 | 0원 |
| 월요일 미션 | 오늘이 화요일, 미완료 | 1 | 1,000원 |
| 월요일 미션 | 수요일에 뒤늦게 완료 | 2 | 2,000원 |
| 월요일 미션 | 오늘이 금요일, 미완료 | 4 | 4,000원 |
| 월요일 미션 | 일요일까지 미완료 (최대치) | 7 | 7,000원 |
| 수요일 미션 | 오늘이 금요일, 미완료 | 2 | 2,000원 |
| 일요일 미션 | 미완료 | 1 | 1,000원 |

> **이 표는 `PenaltyCalculatorTest`의 테스트 케이스와 1:1로 대응해야 한다.**
> 규칙을 바꾸려면 이 표를 먼저 고치고 테스트를 맞춘다.

---

## 4. 정산

- 각 참여자의 벌금 = 해당 주기에 속한 본인 미션들의 벌금 합계
- **총합을 벌금이 더 적은 쪽이 수령한다.**

```
나  3,000원   |   서연  8,000원   |   총 11,000원   →   나 수령
```

실제 현금 흐름으로는 서연이 나에게 8,000원을 주는 것과 같다.
(나는 내 몫 3,000원을 냈다가 총액 11,000원을 돌려받으므로 순증 8,000원)

**동점 처리:** 양쪽 금액이 같으면 정산 없이 각자 회수한다.

---

## 5. 화면

| 경로 | 화면 | 내용 |
|---|---|---|
| `/` | 시작 | 방 만들기 / 초대코드로 참여 |
| `/room/{code}` | 오늘 | 내 오늘 미션 등록·체크 + 상대방 오늘 현황(읽기 전용) |
| `/room/{code}/week` | 주간 보드 | 7일 × 2인 그리드. 이전/다음 주기 이동 |
| `/room/{code}/calendar` | 캘린더 | 월 단위. 날짜를 누르면 그날 미션 상세 |
| `/room/{code}/settle` | 정산 | 미션별 벌금 내역 + 인별 합계 + 총액 + 수령자 |

### UI 규칙

- **미션 제목은 말줄임하지 않는다.** 여러 줄로 그대로 보여준다. 하루 3~5개 규모라
  리스트가 길어지지 않고, 접힌 계획은 읽지 않게 된다.
  예외: 정산 화면처럼 표 형태인 곳만 한 줄로 자른다.
- **오늘 화면에서 밀린 항목 옆에 현재 벌금을 배지로 표시한다** (`2일 밀림 · 2,000원`).
  정산 화면까지 가야 알게 되면 압박이 걸리지 않는다.
- **주간 보드 칸에는 `2/3` 형태의 요약만** 넣는다. 14칸에 제목을 다 넣으면 못 읽는다.
  칸을 누르면 그날 미션 목록이 펼쳐진다.
- **캘린더는 점 하나로 3가지 상태만** 구분한다: 전부 완료 / 미완료 있음 / 미션 없음.
  금액은 넣지 않는다.
- **마감 전(오늘)인 항목은 회색 중립색**으로 표시한다. §3.2의 "당일 유예"가
  UI에서도 보여야 사용자가 헷갈리지 않는다.
- 정산 화면은 **미션별로 며칠 밀렸는지까지** 보여준다. 금액만 보여주면 서로 납득이 안 된다.

---

## 6. 데이터 모델

```
Room    : id, name, inviteCode, startDate
          영구 존속. 주기는 startDate 기준 계산 (§1)

Member  : id, roomId, nickname, token
          token은 쿠키에 저장되어 세션 식별에 쓰인다

Mission : id, roomId, memberId, targetDate, title, done, doneAt
          title은 TEXT (길이 제한 없음)
          인덱스: (roomId, targetDate)  ← 주간/캘린더 조회용
```

**벌금은 테이블에 저장하지 않는다.** 미션 상태만 저장하고 정산은 매번 계산한다.
적립 테이블을 두면 체크 해제 시 동기화 버그가 생긴다.

**용량 걱정은 하지 않아도 된다.** 미션 1행 ≈ 300바이트. 2명 × 5개/일 × 365일 ≈ 1MB.
무료 한도 500MB 기준 500년치다. 과거 기록은 지우지 않는다.

---

## 7. 접속 및 공유

**각자 자기 PC에서 앱을 실행하고, DB만 공유한다.**

```
[내 PC: 앱]  ──┐                      ┌──  [팀원 PC: 앱]
               └──→  Neon Postgres  ←──┘
```

- 서버 포트: **9080**
- 초대 = **코드 문자열을 카톡 등으로 전달**. 팀원은 자기 `localhost:9080`에 접속해 코드 입력.
  링크에 IP나 호스트 주소가 들어가지 않으므로 base-url 설정이 필요 없다.
- 초대코드는 **8자 이상 랜덤**, 방 인원은 **2명으로 제한**.

### DB 접속 정보는 환경변수로

연결 문자열에 **비밀번호가 들어있다. 절대 커밋하지 않는다.**

```properties
spring.datasource.url=${DB_URL}
```

팀원에게는 `DB_URL` 값을 따로 전달한다.

### Hikari 설정 (필수)

Neon 무료 티어는 컴퓨트 100시간/월이고, 5분 유휴 시 자동으로 잠들어 시간을 소모하지 않는다.
그런데 **커넥션 풀이 연결을 붙잡고 있으면 DB가 잠들지 못해** 앱을 켜둔 시간만큼 그대로 차감된다.
앱을 24시간 켜두면 4일 만에 한도가 소진된다. 아래 설정으로 유휴 시 연결을 놓게 한다.

```properties
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.maximum-pool-size=3
```

---

## 8. 기술 스택

| 항목 | 선택 | 이유 |
|---|---|---|
| Java | **21** | 레퍼런스가 가장 많음 (현재 build.gradle은 25 → 낮춰야 함) |
| Spring Boot | **3.5.x** | 상동 (현재 4.1.0 → 낮춰야 함) |
| 화면 | Thymeleaf 서버렌더 | 화면 5장 규모. SPA는 과함 |
| DB | **Neon Postgres** (무료, 카드 불필요) | 각자 로컬 실행 + DB만 공유하는 구조의 핵심 |
| 인증 | 초대코드 + 닉네임 → 쿠키 + 필터 1개 | **Spring Security는 쓰지 않는다.** 규모 대비 설정 비용이 과다 |
| CSS | 단일 `style.css` | 프레임워크 없음 |

---

## 9. 구현 순서

1. **Java 21 / Boot 3.5.x로 낮추기** + 의존성(web, thymeleaf, data-jpa, postgresql)
   + 포트 9080 + Hikari 설정 + **부팅 확인**
2. 엔티티 3개 + 리포지토리
3. **`PenaltyCalculator` + 단위 테스트** ← §3.3 검산표를 그대로 테스트로
4. 방 생성/참여 + 세션 필터
5. 오늘 화면
6. 주간 보드
7. 캘린더
8. 정산 화면
9. CSS 정리

3번을 먼저 못 박아야 이후 화면 작업 중 규칙이 깨져도 `./gradlew test`로 잡힌다.
1번에서 부팅이 확인되기 전에는 다음 단계로 넘어가지 않는다.

---

## 10. 결정된 사항 (2026-08-17 확정)

- **미션은 미리 등록 가능하다.** "오늘" 화면은 당일 등록용으로 남겨두고,
  **주간 보드에서 날짜를 클릭하면 그 날짜로 등록하는 폼**을 제공한다. 일요일 밤에
  한 주 계획을 한 번에 짜는 흐름을 지원하기 위함이다. 캘린더는 여전히 열람 전용이다
  (과거 기록 확인용이라는 원래 취지 유지, 등록 폼을 넣지 않는다).
- **정산 완료(실제로 돈을 주고받았음) 체크 기능은 만들지 않는다.** 팀원끼리 직접 보고
  처리하는 용도라 앱이 추적할 필요가 없다.
